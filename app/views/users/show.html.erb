<div class="container mt-5">
  <div class="row">
    <div class="col-7 mx-auto">
      <div class="card">
        <div class="card-body">
          <h5 class="card-title"><%= current_user.nickname %></h5>
          <h6 class="card-subtitle mb-2 text-muted"><%= current_user.email %></h6>
          <h6 class="card-subtitle mb-2 text-muted"><%= current_user.sex.name %></h6>
          <h6 class="card-subtitle mb-2 text-muted"><%= current_user.generation.name %></h6>
          <h6 class="card-subtitle mb-2 text-muted"><%= current_user.height %> cm</h6>
          <h6 class="card-subtitle mb-2 text-muted"><%= current_user.body_weight %> kg</h6>
          <%= link_to 'ユーザー編集', edit_user_path%>
        </div>    
      </div>
    </div> 
   </div>
 </div>